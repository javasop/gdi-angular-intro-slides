/**
 * Created by osama on 4/4/2015.
 */
var app = angular.module('turing', [ ]);

app.controller('UserController', function($scope){

    //we can optionally initalize any of the scope variables such as myName, myColor, fontSize
    $scope.myName = "Sam"

});
